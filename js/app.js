/*global $ */
/*jshint unused:false */
var app = app || {};


$(function () {
  'use strict';

  // kick things off by creating the `App`
  //new app.AppView({ model: app.AppModel });
  new app.AppView();
});