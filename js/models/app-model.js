/*global Backbone */
var app = app || {};

(function () {
    'use strict';

    app.AppModel = Backbone.Model.extend({
        defaults: {
        },

        initialize: function() {
        }
    });
})();